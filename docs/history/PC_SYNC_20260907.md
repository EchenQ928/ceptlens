# 新 PC 源码同步 · 2026-09-07

后续环境适配：公司主机为 Node 22.18.0，已调整测试依赖、启动要求与锁文件支持该版本。以下记录保留首次同步的验证环境，最新部署要求与验证见 `NODE22_COMPATIBILITY.md`。

## 已接收的更新

以用户传回的 `MODELPATH_BETA4_FULL_PC_DEPLOY_20260904(1).zip` 为新 PC 来源，与 9 月 4 日交付基线逐文件比较后同步至本地 `platform-v1-beta/`。

- 独立内容预览：8766 端口、热更新、独立草稿目录；正式网站仍使用 8765。
- 学习体验：高级筛选、收藏恢复、掌握反馈、最近学习入口、异常进度恢复、小屏导航。
- 教学呈现：词条按需加载、页面错误边界、代码语法高亮、探索返回路径修正。
- 发布服务：单一发布锁、校验/测试/暂存构建、失败恢复、旧静态资源兼容、ZIP 与导入检查。
- 固定依赖版本、更新后的两套内容规范及人工审查记录。

## 内容保留

| 内容 | 题目 | 词条 | 待补词条 | 路径 |
| --- | ---: | ---: | ---: | --- |
| 正式库 | 34 | 25 | 76 | `content-libraries/` |
| 预览草稿 | 34 | 27 | 72 | `.modelpath-lab/content-libraries/` |

预览库新增全连接层与 MLP，重做神经网络、激活函数、损失函数、反向传播。审查记录显示这些内容仍等待人工审查，故保持草稿状态。教学正文按来源保留；自动生成的依赖索引按当前校验器重建。

## 本次兼容修复

1. 启动前按实际依赖要求检查 Node 版本，检查全部直接运行/开发依赖；缺少或版本不一致时执行锁文件安装。修复旧脚本只检查 fflate 而漏装新增依赖的问题。
2. 浏览器单元测试显式使用 jsdom 的存储对象，避免 Node 26 全局 localStorage 覆盖浏览器存储，阻塞内容发布。
3. 恢复 macOS/Linux 启动脚本执行权限，更新部署及内容接入说明。

版本号仍为 `1.0.0-beta.4`，本包是 v1beta 的 2026-09-07 更新快照；题目及词条包格式继续为 3.0，SDK 为 1.x。

## 验证边界

已通过：

- 正式库：34 题、25 词条内容校验，11 个测试文件 / 45 项测试，TypeScript 与生产构建。
- 预览库：34 题、27 词条内容校验，12 个测试文件 / 48 项测试，TypeScript 与生产构建。
- 隔离副本 HTTP 实测：题目导入、词条 ZIP 导出/导入、并发发布拒绝、无效题目回滚、词条构建失败时恢复内容及线上构建。
- 浏览器检查：正式首页和做题页、展开答案再切题时状态重置、预览神经网络节点切换、MLP 草稿正文加载。

本次在 macOS / Node 26.7.0 上执行依赖安装、正式与预览内容校验、单元/交互测试、生产构建及 HTTP 发布验证。Windows 子进程调用与目录交换有自动测试；未在公司 Windows 实机上执行本次包，最终实机环境仍需按部署指南启动验收。

公司传回目录、同步前本地备份分别保留在项目 `incoming/pc-update-20260907/` 和 `backups/before-pc-sync-20260907.tar.gz`；它们不属于可部署程序。运行口令、旧暂存构建和跨系统 node_modules 不进入新压缩包。
